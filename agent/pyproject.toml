[project]
name = "agent"
version = "0.1.0"
description = "Персонаж, который живёт в фоне: биография, сны, побуждения, голос"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "openai>=1.40",
    "psycopg[binary]>=3.1",
    "pymupdf>=1.28.2",
    "python-dotenv>=1.0",
]
